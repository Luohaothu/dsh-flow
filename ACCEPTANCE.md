# dsh-flow 首版验收范围

## 架构

- 独立 flowd 是工作流状态唯一写入者，DSH 插件作为客户端和执行器。
- Workflow 草案经过编译校验后才能进入持久执行；worker 只能凭有效租约提交结果。
- 控制器使用 SQLite 事务；界面关闭不影响任务，重启不丢状态。
- DSH 主服务不为测试重启；优先用隔离 profile 验证后再安装。

## 首版必须实际验证

1. 工作流 DAG：独立节点并发、依赖节点等待、输出持久化。
2. 编译拒绝环、缺失依赖、未知能力和超限请求。
3. 重复提交幂等，重复领取不产生双重有效租约。
4. worker 崩溃/租约过期后有界重试；旧 epoch 结果不能晋升。
5. pause 停止新派发、resume 继续、cancel 拒绝迟到提交。
6. controller 进程重启后可继续执行；失败不能伪装成功。
7. DSH 插件在已安装版本中真实加载；工具和 UI 使用实际公共 API。
8. 控制器需要 bearer token；浏览器不能取得控制器 token。
9. 使用独立测试环境验证 UI 和多 worker 端到端链路。
10. 区分确定性 worker 测试、测试替身和真实模型调用。无模型凭据时明确列出未测项目。

## 明确不冒充已实现的后续范围

- 完整 Mission/WorkItem/Iteration 四阶段自动规划。
- phase supervisor 动态合成、GraphPatch 和下游失效传播。
- 不受信任代码执行的完整容器/网络隔离。
- 跨主机高可用、多控制器与端到端 exactly-once 外部副作用。
- 新能力自动生成/准入和自定义 agent-loop。

这些不是已验收首版功能；若后续实现，需要新增故障注入和集成测试。
